import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.selfwish.app',
  appName: 'Selfwish',
  webDir: 'dist/selfish-angular',
  bundledWebRuntime: false
};

export default config;
